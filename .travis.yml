language: python
python:
  - "2.7"
  - "3.4"
  - "3.5"
  - "3.6"
# command to install dependencies
install:
  - pip install -r pip_requirements_readthedocs.txt
  - pip install -r pip_requirements_tests.txt
# command to run tests
script:
  - set -e
  - python --version
  - python -m test/run_all.py

after_success:
